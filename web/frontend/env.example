# Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# API Configuration
# =============================================================================

# Your Cloudflare Worker API URL (backend)
# Production: https://your-worker.your-subdomain.workers.dev
# Development: http://localhost:8787
NEXT_PUBLIC_API_URL=https://your-cloudflare-worker-url.workers.dev

# =============================================================================
# NextAuth Configuration
# =============================================================================

# The base URL of your application
# Production: https://your-app.vercel.app
# Development: http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Secret for signing JWT tokens
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-here

# =============================================================================
# GitHub OAuth Application
# =============================================================================

# Create OAuth app at: https://github.com/settings/developers
# Homepage URL: http://localhost:3000 (dev) or your production URL
# Callback URL: http://localhost:3000/api/auth/callback/github

# GitHub OAuth App Client ID
AUTH_GITHUB_ID=your-github-oauth-client-id

# GitHub OAuth App Client Secret
AUTH_GITHUB_SECRET=your-github-oauth-client-secret

# =============================================================================
# Backend Communication
# =============================================================================

# Shared secret for authenticating requests to backend
# Must match SERVER_SECRET in your Cloudflare Worker
# Generate with: openssl rand -hex 32
SERVER_SECRET=your-shared-server-secret

# =============================================================================
# Setup Instructions
# =============================================================================

# 1. Copy this file:
#    cp env.example .env.local

# 2. Generate secrets:
#    openssl rand -base64 32  # for NEXTAUTH_SECRET
#    openssl rand -hex 32     # for SERVER_SECRET

# 3. Create GitHub OAuth App:
#    - Go to https://github.com/settings/developers
#    - Click "New OAuth App"
#    - Fill in the details and get your Client ID and Secret

# 4. Deploy Cloudflare Worker first to get the API URL

# 5. Update all values in .env.local

# 6. Run: npm run dev
